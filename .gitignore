# ── Secrets — NEVER commit ──
.env
.env.*
!.env.example
*.pem
credentials.json
service-account.json

# ── Perfis Chrome do bot do Meet (sessao logada — NUNCA commitar) ──
.chrome_meet_profile/
.chrome_meet_*/

# ── Diagnosticos / screenshots do bot e testes locais ──
_ImagesPrj/
vexa-bot-check/

# ── Logs (contem transcricoes de reuniao — NUNCA commitar) ──
logs/

# ── Python ──
venv/
venv_*/
__pycache__/
*.pyc
.pytest_cache/

# ── Node / Electron ──
node_modules/
dist/
out/

# ── Generated audio / artifacts ──
artifacts/
*.mp3
*.wav
!assets/**/*.mp3
!assets/**/*.wav

# ── OS ──
Thumbs.db
.DS_Store
